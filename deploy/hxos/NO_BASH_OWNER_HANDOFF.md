# No-Bash owner handoff

The owner does not need to run shell commands for this activation path.

The only required owner interaction is external host authentication/authorization if the hosting provider is not already connected to HXOS. After authentication, deployment should continue using provider UI/API and automated health checks.
